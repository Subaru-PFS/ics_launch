actors="meb peb tests fps sunss sps iic pfilamps"
(date; echo "launching ${actors}....") > /tmp/pfslaunch.log

for a in $actors; do
    (
	source /software/ics_launch/bin/setup.sh

	setupForHost -v ics_${a}Actor
	stageManager $a restart
    ) >> /tmp/pfslaunch.log 2>&1
done
